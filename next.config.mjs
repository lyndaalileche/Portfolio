/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'bvdcxrlauxbutqunjjww.supabase.co',
      },
    ],
  },
};

export default nextConfig;

